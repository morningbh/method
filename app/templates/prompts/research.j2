/research-method-designer

用户的研究问题：
{{ question }}

{% if uploaded_files -%}
用户上传了以下资料（相对于当前工作目录）：
{% for f in uploaded_files -%}
{% if f.kind == "pdf_scan" -%}
- {{ f.original_name }} → {{ f.local_path }} (PDF 文本层为空，可能为扫描件/图片 PDF；请直接用 Read 工具打开原 PDF，Claude Read 支持 PDF 视觉读取)
{% elif f.kind == "image" -%}
- {{ f.original_name }} → {{ f.local_path }} (图片文件；请用 Read 工具直接视觉读取)
{% elif f.kind == "failed" -%}
- {{ f.original_name }} → {{ f.local_path }} (解析失败，已忽略)
{% else -%}
- {{ f.original_name }} → {{ f.local_path }}
{% endif -%}
{% endfor %}
请根据需要用 Read 工具阅读这些文件，把相关内容纳入研究方案的"已有材料"部分。

{% endif -%}
## 输出硬性要求（优先于 skill 的其它表达风格）

1. **全中文输出**。不要出现任何英文句子、英文段落、英文前言、英文总结。涉及的英文专有名词（公司名、技术名词）可保留原文，但句子主体必须用中文。
2. **不要任何 meta 解释/前言**。不写"Using the research-method-designer skill..."、"Let me produce..."、"I'll treat this as..."、"Below is..." 之类的元话术。直接从第一节标题（`# 研究方案：...` 或 `## 1. 问题重述`）开始。
3. **移动端友好的排版**。读者用手机读。因此：
   - **表格用得克制**：只在真正需要并排对比 3+ 项时才用表格；表格最多 3 列。能改成短条目列表就改。
   - 段落短，一段 2-4 句。长句断开。
   - 标题层级清晰（`##` / `###`），避免四级标题。
   - 代码块、引号块少用，必要时用。
   - 列表用 `-` 或 `1.`，缩进不超过 2 层。
4. **不要使用内部分类符号**。禁止在输出里出现 "Type A"、"Type B"、"Type C"、"Type D"、"类型 A"、"类型 B"、"类型 C"、"类型 D" 这类记号。需要提及问题类型时，用自然中文称呼（例如"观察对比型问题"、"找原因型问题"、"推演未来型问题"、"取舍决策型问题"），或者直接用一句话自然描述这是一个什么样的问题。
5. **术语口语化**。避免学术 / 方法论黑话：不写"命题图谱"、"抽取字段"、"制品深挖"、"证据评分量表"、"对立机制"、"跨案例编码"；改用"猜想清单"、"每个案例要记哪些字段"、"挑几个典型案例细看"、"证据可信度打分"、"竞争性解释"、"横向归类"等更直白的说法。其他学术味重的词也照此原则改写。
6. **不要单独开"问题类型分类"章节**。skill 要求的"第 3 节 question-type classification" 是你内部用来挑方法的元信息，不要作为独立一级章节写给用户看；如需让用户知道这是什么类问题，用一句话嵌进"问题重述"或"方法选择理由"段落即可。其他章节正常产出。

直接产出完整的中文研究方案 markdown（按 skill 要求的章节结构，但遵守上述六条硬性要求；其中第 6 条允许你省略"问题类型分类"章节）。
