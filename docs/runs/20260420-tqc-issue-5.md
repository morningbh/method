# Test Quality Check — Issue #5 (frontend UX error copy)

- **Date**: 2026-04-20
- **Design doc**: `/home/ubuntu/method-dev/docs/design/issue-5-error-copy.md`
- **Tester report**: `/home/ubuntu/method-dev/docs/runs/20260420-tester-issue-5.md`
- **Rubric**: universal 1-12 + Method-13 (design coverage BLOCKING) + 14-16 + project-verbatim (BLOCKING)
- **Files audited**:
  - `/home/ubuntu/method-dev/tests/unit/test_error_copy.py`
  - `/home/ubuntu/method-dev/tests/unit/test_file_processor_error_copy.py`
  - `/home/ubuntu/method-dev/tests/integration/test_auth_error_copy.py`
  - `/home/ubuntu/method-dev/tests/integration/test_research_error_copy.py`
  - `/home/ubuntu/method-dev/tests/test_static_assets.py`

## Summary

- **Final VERDICT**: **PASS**
- Design coverage: **13 / 13**
- BLOCKING issues: **0**
- WARN issues: **3**
- Auto-rejection criteria triggered: **NONE**

---

## 1. Per-file rubric verdicts

### 1.1 `tests/unit/test_error_copy.py`

| # | Rubric item | Verdict | Note |
|---|---|---|---|
| 1 | Tests read contracts only, not implementation | PASS | Imports `app.services.error_copy` symbols; no peek into implementation body. |
| 2 | Assertions specific (not truthiness) | PASS | Every check is `==` equality against design strings. |
| 3 | No over-mocking (mock-boundary gap) | PASS | Zero mocks; pure dict/function under test. |
| 4 | Edge cases present | PASS | Unknown code + empty-string code both assert the fallback `"操作失败，请稍后重试"`. |
| 5 | Parametrize used where appropriate | PASS | Two 24-code parametrizes over `ERROR_COPY.items()` and `message_for`. |
| 6 | RED by construction | PASS | Module `app/services/error_copy` does not exist; import will fail pending GREEN. |
| 7 | No test interdependence | PASS | Each function independent. |
| 8 | Docstrings reference design §§ | PASS | §4.1 / §5 cited. |
| 9 | No dead/commented code | PASS | — |
| 10 | Names match design vocabulary | PASS | "ERROR_COPY", "message_for", same keys as design. |
| 11 | Parametrize ids are meaningful | PASS | `ids=sorted(EXPECTED_COPY.keys())` gives per-code ids. |
| 12 | No integration/unit confusion | PASS | Pure unit. |
| verbatim | Chinese strings char-exact | PASS | All 24 entries transcribed verbatim (see §3 below). |

### 1.2 `tests/unit/test_file_processor_error_copy.py`

| # | Rubric item | Verdict | Note |
|---|---|---|---|
| 1 | Contracts only | PASS | Imports `LimitExceededError`, `validate_upload_limits`, `save_and_extract` — public API surface; no private helper poking. |
| 2 | Specific assertions | PASS | Each of 6 branches asserts `detail == {"error": <code>, "message": <verbatim>}`. |
| 3 | Mock-boundary gap | PASS | Uses real `validate_upload_limits` / `save_and_extract`; only the FastAPI `UploadFile` is swapped for a minimal `_FakeUploadFile` stub — acceptable because this is the standardized test seam used in the existing `test_file_processor.py`. No DB touched. |
| 4 | Edge cases | PASS | Empty-string `message` → falls back to `message_for(code)`; explicit non-empty `message` preserved verbatim. |
| 5 | Parametrize | WARN(minor) | 6 branches written as 6 sibling `async def` tests rather than one parametrize — readable but slightly verbose. Not a correctness issue; no action required. |
| 6 | RED | PASS | New `{error,message}` detail shape doesn't yet exist in `LimitExceededError`; legacy `code` key sweep will fail until migration. |
| 7 | Independence | PASS | Each raises its own exception. |
| 8 | Docstrings cite §§ | PASS | §3.2, §4.1, §5 named. |
| 9 | No dead code | PASS | — |
| 10 | Vocabulary match | PASS | All 6 codes match §4.1. |
| 11 | IDs / names | PASS | Function names describe the branch. |
| 12 | Unit placement | PASS | File-system touched via `upload_dir` fixture only for `mime_mismatch` which genuinely needs disk I/O; rest is pure. |
| verbatim | Char-exact | PASS | All 6 MSG_* constants match §4.1 character-for-character, inc. full-width punctuation `，` and parentheses `（0 字节）`. |
| legacy key tripwire | — | PASS | `assert "code" not in detail` present in 2 spots (branch 1 + explicit BC sweep). |

### 1.3 `tests/integration/test_auth_error_copy.py`

| # | Rubric item | Verdict | Note |
|---|---|---|---|
| 1 | Contracts only | PASS | No read of `app/routers/auth.py`; only HTTP + fixtures. |
| 2 | Specific assertions | PASS | Every test asserts `body.get("error") == <code>` AND `body.get("message") == MSG_*`. |
| 3 | Mock-boundary gap (Item 14) | PASS | `mailer_mocks` / `failing_login_mailer` patch SMTP only (outside process boundary); the DB path is real (SQLite tmp). Every mocked function is SMTP, which is orthogonal to the assertions. |
| 4 | Edge cases | PASS | `invalid_or_expired` covered by two cases: wrong code + expired code (same error code/message). |
| 5 | Parametrize | INFO | 5 separate tests — acceptable since fixtures differ across cases (different mailer, different seeded state). |
| 6 | RED | PASS | Assertions require `message` field which routes don't yet emit. |
| 7 | Independence | PASS | Each test has its own fresh DB via `app_client` fixture. |
| 8 | Docstrings cite §§ | PASS | §3.2, §4.1, §5 named. |
| 9 | No dead code | PASS | — |
| 10 | Vocabulary match | PASS | All 5 auth codes match §4.1 ids. |
| 11 | Test names descriptive | PASS | Names encode code + expected fields. |
| 12 | Integration placement | PASS | Hits real FastAPI routes via `httpx.AsyncClient`. |
| BC (design §3.2) | — | PASS | `test_error_field_still_present_after_message_addition` asserts BOTH keys present. |
| verbatim | Char-exact | PASS | `MSG_RATE_LIMIT`, `MSG_MAIL_SEND_FAILED`, `MSG_INVALID_OR_EXPIRED`, `MSG_UNAUTHENTICATED`, `MSG_BAD_ORIGIN` all match design §4.1. |

### 1.4 `tests/integration/test_research_error_copy.py`

| # | Rubric item | Verdict | Note |
|---|---|---|---|
| 1 | Contracts only | PASS | Uses `app.models` for DB seeding (which is declared contract surface) and `research_runner.stream` monkeypatch as a well-known test seam. No read of `app/routers/*`. |
| 2 | Specific assertions | PASS | Every body assertion is `==` against verbatim §4.1 Chinese. |
| 3 | Mock-boundary gap (Item 14) | PASS | `research_runner.stream` swapped to a no-op (prevents real subprocess); the router's validation and DB logic are real. Request-time validation paths are the thing being tested, so the swap is orthogonal. |
| 4 | Edge cases | PASS | Global 404 handler exercised via unmatched route (test #14 covers the default-404 path, not just explicit `HTTPException`). |
| 5 | Parametrize | INFO | 18 separate tests. Each has distinct seed state, so parametrize would not add value. |
| 6 | RED | PASS | Assertions require the migrated `{error,message}` shape and the `main.py` wrapping handler, neither of which exists yet. |
| 7 | Independence | PASS | Each test seeds and clears its own cookies. |
| 8 | Docstrings cite §§ | PASS | §3.2, §4.1, §5, §7-note-1 named. |
| 9 | No dead code | PASS | The `_install_noop_stream` helper is reused across tests. |
| 10 | Vocabulary match | PASS | All 13 research/history/404/file codes covered match §4.1 ids. |
| 11 | Names descriptive | PASS | — |
| 12 | Integration placement | PASS | Real routes + real DB + real Jinja template rendering. |
| xfail discipline | — | PASS | `test_research_internal_500_returns_error_and_message` uses `xfail(reason=...)` with the expected body spelled out (per TESTER_PROMPT preference over silent skip). |
| template coverage | — | PASS | Two tests on `templates/history_detail.html`: one for the `error_message` present path (verbatim), one for `error_message=None` (fallback `"研究失败，原因未知，请重试"`). |
| BC tripwire | — | PASS | `test_post_research_too_many_files_returns_new_shape` asserts `"code" not in body`. |
| L4 reverse-scan hygiene | — | PASS | The global-404 path + the explicit-HTTPException path + the routes' own `JSONResponse` path are all exercised, closing the "migrated some files, missed others" failure mode. |
| verbatim | Char-exact | PASS | All 14 MSG_* constants match design §4.1 character-for-character (see §3 sample). |

### 1.5 `tests/test_static_assets.py`

| # | Rubric item | Verdict | Note |
|---|---|---|---|
| 1 | Contracts only | PASS | Reads `app/static/app.js` as **text**, never imports / executes it. This is exactly the "review-by-grep" strategy design §5 and §7-note-3 pin down. |
| 2 | Specific assertions | PASS | Regex patterns are explicit about what's forbidden (`alert(...+<x>.error)`) and what's required (`showError`, `showNetworkError`, two fallback strings). |
| 3 | Mock-boundary gap | N/A | Static grep test — no I/O other than file read. |
| 4 | Edge cases | PASS | Parametrized for the three known variable spellings (`body`, `data`, `resBody`) — design §5 names all three. |
| 5 | Parametrize | PASS | `variable_name` parametrize with readable ids. |
| 6 | RED | PASS | `app.js` currently has the `alert(... + body.error)` pattern at lines 35 / 60 / 673 per design §7 — tests will fail until migration. `showError` / `showNetworkError` don't exist yet. |
| 7 | Independence | PASS | Each test re-reads source file. |
| 8 | Docstrings cite §§ | PASS | §5 / §6.1 / §7 named. |
| 9 | No dead code | PASS | — |
| 10 | Regex rigor | PASS | Regex allows whitespace variation (`\s*`) and covers both `alert( "..." + body.error)` and `alert( "..." + (body.error || ...))`. |
| 11 | Names descriptive | PASS | — |
| 12 | Placement | PASS | `tests/test_static_assets.py` — shared (not unit/integration); runs on file bytes. |
| L4 integration-path discipline | — | PASS | Implements the CI tripwire the rule book demands ("grep `<old_symbol>` and fail if any match remains"). |
| invocation-check | — | PASS | `test_show_error_helper_is_actually_invoked` asserts `≥3` call sites — guards against "helper defined but never called = dead code". |

---

## 2. Design coverage table (§5 — 13 output rows)

| # | Design output file | Covering test(s) | Status |
|---|---|---|---|
| 1 | `app/services/error_copy.py` | `tests/unit/test_error_copy.py::test_module_imports_and_exports_dict_and_helper`, `test_error_copy_keys_equal_design_set`, `test_error_copy_values_match_design_exactly[*]` (24), `test_message_for_returns_design_string[*]` (24), `test_message_for_unknown_code_returns_generic_fallback`, `test_message_for_empty_string_returns_generic_fallback`. Also imported by `tests/unit/test_file_processor_error_copy.py::test_limit_exceeded_error_empty_message_falls_back_to_lookup`. | COVERED |
| 2 | `app/routers/auth.py` | `tests/integration/test_auth_error_copy.py` — 7 tests covering all 5 §4.1 auth codes + BC. | COVERED |
| 3 | `app/routers/research.py` | `tests/integration/test_research_error_copy.py` — 13 tests covering `empty_question`, `question_too_long`, `invalid_mode`, `plan_missing`, `request_busy`, `request_not_finalized`, `anchor_text_invalid`, `body_invalid`, `anchor_context_too_long`, `body_empty`, `ai_reply_not_deletable`, `not_found` (via download), `internal` (xfail). | COVERED |
| 4 | `app/routers/history.py` | `tests/integration/test_research_error_copy.py::test_history_detail_not_found_returns_error_and_message` (GET `/api/history/<missing>`). | COVERED |
| 5 | `app/services/file_processor.py` | `tests/unit/test_file_processor_error_copy.py` — all 6 `LimitExceededError` branches + 2 constructor semantics. Integration bubble: `tests/integration/test_research_error_copy.py::test_post_research_too_many_files_returns_new_shape`. | COVERED |
| 6 | `app/main.py` (global HTTPException handler) | `tests/integration/test_research_error_copy.py::test_global_404_handler_returns_error_and_message`, `test_research_download_not_found_returns_error_and_message`, `test_history_detail_not_found_returns_error_and_message`. | COVERED |
| 7 | `app/static/app.js` | `tests/test_static_assets.py` — 9 grep-based cases: forbidden pattern absence (3 variable spellings), `showError` + `showNetworkError` definition, required Chinese copy presence, actual invocation count ≥ 3. | COVERED |
| 8 | `app/templates/history_detail.html` | `tests/integration/test_research_error_copy.py::test_history_detail_failed_with_error_message_renders_verbatim`, `test_history_detail_failed_with_null_error_message_renders_fallback`. | COVERED |
| 9 | `tests/test_error_copy.py` (new) | `tests/unit/test_error_copy.py` — self-covering (salvaged from prior run; file name prefix `test_error_copy.py` matches). | COVERED (placement note ↓) |
| 10 | `tests/test_static_assets.py` (new) | `tests/test_static_assets.py` — self-covering. | COVERED |
| 11 | `tests/routers/test_auth_error_copy.py` (new) | `tests/integration/test_auth_error_copy.py` — self-covering (placement note ↓). | COVERED |
| 12 | `tests/routers/test_research_error_copy.py` (new) | `tests/integration/test_research_error_copy.py` — self-covering (placement note ↓). | COVERED |
| 13 | `tests/services/test_file_processor_error_copy.py` (new) | `tests/unit/test_file_processor_error_copy.py` — self-covering (placement note ↓). | COVERED |

**Coverage: 13 / 13.**

**Placement WARN** (rows 9, 11, 12, 13, tester Assumption §1): design §5 literal paths are `tests/routers/...` / `tests/services/...`; tester placed under `tests/integration/` and `tests/unit/` to inherit the shared fixtures in `tests/integration/conftest.py` (`app_client`, `auth_session`, `mailer_mocks`, `integration_db`, `seeded_user`, `seed_login_code`, `pinned_admin_email`, `failing_login_mailer`, `research_paths`) without duplicating them. Rubric-item-13 explicitly allows this: coverage is preserved, the filename prefixes match (`test_auth_error_copy.py`, `test_research_error_copy.py`, `test_file_processor_error_copy.py`), and moving them into `tests/routers/` / `tests/services/` would force fixture duplication with no coverage gain. **Classified WARN (placement only), not FAIL.**

---

## 3. Verbatim Chinese copy spot-check

Sampled 7 of the 24 §4.1 codes + the 2 design §4.2 front-end fallback strings + the §4.3 template fallback. All match character-for-character including full-width punctuation (`，`, `（`, `）`), full-width digits, and half-width ASCII/latin characters inside the message (`md/txt/pdf/...`, `50 MB`, `0 字节`).

| Code | Design §4.1 string | Test assertion | Match |
|---|---|---|---|
| `rate_limit` | `请求过于频繁，请稍后再试` | `test_auth_error_copy.py:27 MSG_RATE_LIMIT` + unit-test `EXPECTED_COPY`:24 | ✓ |
| `empty_question` | `请输入研究问题` | `test_research_error_copy.py:35 MSG_EMPTY_QUESTION` + unit EXPECTED_COPY:31 | ✓ |
| `request_busy` | `请求仍在处理中，请等它结束后再操作` | `test_research_error_copy.py:40 MSG_REQUEST_BUSY` + unit:36 | ✓ |
| `anchor_context_too_long` | `选中段落上下文过长，请缩短后重试` | `test_research_error_copy.py:44 MSG_ANCHOR_CONTEXT_TOO_LONG` + unit:40 | ✓ |
| `files_too_many` | `上传文件数超出单次 8 个的上限，请删减后重试` | `test_file_processor_error_copy.py:31-33 MSG_FILES_TOO_MANY` + unit:46 | ✓ |
| `unsupported_type` | `文件类型不支持，请改为 md/txt/pdf/docx/pptx/xlsx/png/jpg/jpeg/webp/gif` | `test_file_processor_error_copy.py:34-36 MSG_UNSUPPORTED_TYPE` + unit:47-49 | ✓ (full MIME list intact, `md/txt/pdf/docx/pptx/xlsx/png/jpg/jpeg/webp/gif` is identical) |
| `empty_file` | `上传的文件是空的（0 字节），请检查后重试` | `test_file_processor_error_copy.py:37 MSG_EMPTY_FILE` + unit:50 | ✓ (full-width parens and comma) |
| `not_found` | `记录不存在或已被删除` | `test_research_error_copy.py:47 MSG_NOT_FOUND` + unit:44 | ✓ |
| §4.2 network | `网络异常，请检查连接后重试` | `test_static_assets.py:123` | ✓ |
| §4.2 5xx fallback | `服务器开小差了，请稍后重试` | `test_static_assets.py:131` (also unit:34 MSG_INTERNAL) | ✓ |
| §4.3 template fallback | `研究失败，原因未知，请重试` | `test_research_error_copy.py:705, 738` | ✓ |
| unknown-code generic fallback | `操作失败，请稍后重试` | `test_error_copy.py:109, 116` | ✓ |

**No copy drift detected.** The tester derived the oracle dictionary `EXPECTED_COPY` from the design §4.1 table verbatim; every integration test's `MSG_*` constant is a transcription of the same source; cross-file consistency confirmed.

---

## 4. Item-level rubric cross-check

| Item | Description | Result | Note |
|---|---|---|---|
| 1 | Tests read contracts only | PASS | No test imports from `app/routers/*`, `app/main.py` body, or private helpers. |
| 2 | Assertions specific, not truthy | PASS | All body-field assertions are `==` against verbatim strings. The two `in` checks are BC tripwires accompanying an adjacent `==` check, not standalone. |
| 3 | No over-mocking | PASS | Mocks limited to (a) SMTP in `mailer_mocks`/`failing_login_mailer`, (b) `research_runner.stream` no-op, (c) `UploadFile` minimal stub. None of these are the code path under assertion. |
| 4 | Edge cases present | PASS | Empty string, unknown code, whitespace-only question, expired code, null `error_message`, exceed-limit file sizes, multiple variable-name spellings — all covered. |
| 5 | Parametrize where appropriate | PASS | Unit dict-lookup + static-asset variable-name; 6 file-processor branches separated for clarity. |
| 6 | RED status | PASS | Tester confirms via static inspection: symbols like `app.services.error_copy`, `showError`, migrated `LimitExceededError.detail` shape don't exist yet. Verified: `ERROR_COPY` not in app/services tree. |
| 7 | No cross-test state | PASS | Each test fresh-seeds its own user/request IDs (all distinct emails, fresh tmp DB per `app_client`). |
| 8 | Docstrings cite design | PASS | Every test header cites §-number. |
| 9 | No dead/commented code | PASS | — |
| 10 | Names match design vocabulary | PASS | Test names + `MSG_*` constant names correspond to §4.1 code ids. |
| 11 | Parametrize ids meaningful | PASS | `ids=sorted(EXPECTED_COPY.keys())` etc. |
| 12 | No integration-as-unit | PASS | — |
| **13** | **Design coverage BLOCKING** | **PASS** | **13 / 13 (with placement WARN)** — see §2. |
| **14** | **Mock-boundary gap BLOCKING** | **PASS** | Nothing DB-touching is mocked in a way that would paper over a real bug. SMTP mocks go through the canonical `auth_flow` seam the existing 2.4 tests use. |
| 15 | Feature-flag branches | N/A | Issue #5 has no feature-flag gating. |
| 16 | Lazy imports in tests | WARN(minor) | `tests/integration/test_research_error_copy.py:467 from app.models import Comment` is a function-local import — acceptable because `Comment` may not exist in all RED-phase checkouts and this defers the import error to the single relevant test. No rule breach. |
| verbatim | Chinese char-exact BLOCKING | PASS | See §3. |

---

## 5. Auto-rejection criteria

| Criterion | Triggered? | Notes |
|---|---|---|
| Missing design-output file with zero covering test | NO | All 13 rows covered. |
| Any asserted `message` field uses `in` / truthy / substring instead of `==` | NO | All primary assertions are `==`. |
| Any Chinese string mismatches design §4.1 | NO | Spot-check clean. |
| Any test obviously GREEN at submission time (missing RED) | NO | Tester confirmed RED; symbols don't yet exist. |
| Mock-boundary gap on a DB-touching function without a paired real-DB test | NO | Integration tests all run on real tmp SQLite. |
| Tests read implementation files | NO | Grep confirmed no imports from `app/routers/*` or `app/main.py` body. |

---

## 6. WARN summary (non-blocking)

1. **Placement deviation** (§2 note, rows 9/11/12/13). Tester placed files at `tests/integration/...` and `tests/unit/...` instead of `tests/routers/...` / `tests/services/...` to inherit fixtures. Acceptable per rubric-item-13 — flagging for reviewer awareness.
2. **6 file-processor branches not parametrized** (§1.2 Item 5). Minor verbosity; not a correctness concern.
3. **Function-local import in `test_delete_ai_reply_returns_error_and_message`** (§4 Item 16). Justified (defers `Comment` model import to the one test that uses it) but technically a lazy-import pattern. No action required.

---

## 7. Final verdict

**PASS.**

The test suite is design-driven (oracle derived from §4.1 verbatim), covers all 13 design-output rows, enforces BC tripwires for the legacy `"code"` key, and implements the L4 reverse-scan CI tripwire for the `app.js` migration. Chinese strings match design character-for-character. No BLOCKING criteria triggered.

Dispatcher may proceed to Step 6 (backup + branch) and Step 7 (dev loop). Per Method HARNESS §§ 10-step adjustment, Step 5 (human test review) is skipped.

---

## 8. Next step

Proceed to **Step 6**: backup target files + create feature branch `feat/issue-5-error-copy`, then Step 7 dev loop until all tests are GREEN within the 20-iteration budget confirmed in Step 1.
